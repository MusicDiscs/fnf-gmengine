if inst != undefined {audio_destroy_stream(inst)}
if vocalbf != undefined {audio_destroy_stream(vocalbf)}
if vocaldad != undefined {audio_destroy_stream(vocaldad)}
if inst_file != undefined {audio_destroy_stream(inst_file)}
if vocalbf_file != undefined {audio_destroy_stream(vocalbf_file)}
if vocaldad_file != undefined {audio_destroy_stream(vocaldad_file)}